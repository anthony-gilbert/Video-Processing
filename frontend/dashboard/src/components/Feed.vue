<template>
    <div class="feed">
        <!-- <div v-if="loading" class="loading">Loading...</div>
        <div v-else>
        <div v-for="item in feedItems" :key="item.id" class="feed-item">
            <h3>{{ item.title }}</h3>
            <p>{{ item.content }}</p>
        </div>
        </div> -->
        Feed component works!
    </div>
</template>